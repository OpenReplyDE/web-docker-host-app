version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "//npm.pkg.github.com/:_authToken=${NPM_TOKEN}" > .npmrc
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
